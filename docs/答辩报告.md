# 多智能体协作系统 — 答辩报告

> **基于 LangGraph + LangChain 的七角色多智能体协作系统**
>
> 西北农林科技大学 · 2026 人工智能实训 · 独立拓展项目
>
> **难度系数：★★★★★** | 版本：v3.4 | 日期：2026-06-24

---

## 目录

1. [项目概述](#一项目概述)
2. [需求对标](#二需求对标)
3. [版本演进](#三版本演进)
4. [系统架构](#四系统架构)
5. [关键设计决策](#五关键设计决策)
6. [功能验证](#六功能验证)
7. [v3.4 核心亮点](#七v34-核心亮点)
8. [技术栈](#八技术栈)
9. [总结与展望](#九总结与展望)

---

## 一、项目概述

### 1.1 项目简介

本项目构建了一个**由 7 个专业角色 Agent 组成的多智能体协作系统**，包含规划 Agent、检索 Agent、执行 Agent、编程 Agent、写作 Agent、评审 Agent 和总结 Agent。用户用自然语言描述需求，系统通过 **Router 意图分类器**自动识别任务类型和复杂度，由 **LangGraph 状态图**编排多个 Agent 按流程协作，最终输出结构化任务报告与完整执行日志。

v3.4 新增**Docker 沙箱与安全加固**：代码执行升级为 Docker 容器隔离，新增 Token 7 天过期自动续期机制与登录限流防暴力破解，安全防护从 7 层提升至 10 层。

### 1.2 核心能力一览

| 能力维度 | 实现方式 |
|----------|----------|
| **多角色协作** | 7 个专业 Agent，LangGraph StateGraph 统一编排 |
| **意图识别** | Router LLM 自动分类 5 种任务类型 + 轻/重复杂度 + 关键词覆写安全网 |
| **任务拆解** | Planner Agent 自动将复杂任务拆解为可执行步骤清单 |
| **知识检索** | ChromaDB 向量数据库 + BAAI/bge-small-zh-v1.5 嵌入模型，按用户物理隔离 |
| **代码执行** | subprocess 沙箱隔离执行，exitcode 驱动确定性分支路由 |
| **结果校验** | Tester Agent 代码评审 + 文档质量检查 + ≤2 轮自动修复循环 |
| **工具链协同** | 7 个 LangChain Tool：搜索、计算、文件读写、数据分析、可视化、OCR |
| **结构化报告** | Summarizer Agent 汇总全流程，输出 Markdown 报告 + 执行日志 |
| **Web 交互** | FastAPI + Jinja2 + daisyUI + Tailwind CSS 现代 Web 界面 |
| **用户认证** | bcrypt 密码哈希 + UUID Token 鉴权，注册/登录/退出完整流程 |
| **用户隔离** | 会话归属校验（403 拦截跨用户访问），知识库按 user_id 物理分目录 |
| **游客模式** | 无需注册即可聊天，对话存 sessionStorage，关标签页即清除，登录可迁移 |
| **Docker 沙箱** | 代码执行在 Docker 容器中隔离运行，不可用时自动降级为 subprocess |
| **登录限流** | 同一用户名 5 次失败后 15 分钟锁定，防暴力破解 |
| **Token 过期** | Token 7 天自动过期，活跃使用中自动续期，兼顾安全与体验 |

---

## 二、需求对标

### 2.1 需求覆盖矩阵

| # | 任务书要求 | 实现情况 | 对应模块 |
|---|-----------|:--------:|----------|
| 1 | 使用 LangGraph/CrewAI 搭建多智能体框架 | ✅ **LangGraph ≥0.2** StateGraph | `workflow.py` |
| 2 | 定义角色分工（规划、检索、计算、写作、评审） | ✅ **7 个 Agent**，超出要求 | `agents.py` |
| 3 | 集成 RAG 知识库，支持任务相关资料检索 | ✅ **ChromaDB + bge-small-zh** + 用户隔离 | `rag/knowledge_base.py` |
| 4 | 实现任务拆解、步骤执行、结果校验闭环 | ✅ **Planner → Executor → Tester → 修复循环** | `workflow.py` |
| 5 | 接入工具集（搜索、计算、文件读写、数据分析） | ✅ **7 个 LangChain Tool** | `tools.py` |
| 6 | 输出结构化任务报告与执行日志 | ✅ **Summarizer 报告 + speaking_log** | `workflow.py` |
| 7 | 所需技术：Python, LangGraph/CrewAI, LLM, RAG, Function Calling, Streamlit | ✅ 全部满足，**Streamlit 升级为 FastAPI Web** | 全项目 |

### 2.2 超出任务书要求的扩展

| 扩展项 | 说明 |
|--------|------|
| **Router 意图分类器** | 自动识别 5 种任务类型 + 轻/重复杂度 |
| **快慢双车道** | 轻任务秒级回复，重任务完整流水线 |
| **exitcode 确定性分支** | 用操作系统退出码替代 LLM 文字判断做路由 |
| **关键词覆写安全网** | 三层正则检测兜底 Router 误判 |
| **OCR 图片识别** | Tesseract 中英文 OCR，图片文字自动入库 |
| **SQLite 持久化** | 用户系统 + 会话历史 + Token 管理 |
| **用户认证系统** | bcrypt 密码哈希 + UUID Token 鉴权，注册/登录/退出 |
| **用户隔离体系** | 会话归属校验 + 知识库物理分目录 |
| **游客模式** | sessionStorage 本地存储，登录后自动迁移 |
| **daisyUI 现代前端** | 响应式抽屉布局 + 折叠面板 + 30+ 主题 |
| **自动化测试** | 9 个 pytest 测试用例覆盖认证 + 知识库 |
| **Docker 沙箱隔离** | subprocess 升级为 Docker 容器隔离，不可用时自动降级 |
| **登录限流机制** | 5 次失败 / 15 分钟锁定，防暴力破解 |
| **Token 过期与续期** | 7 天自动过期 + 活跃续期，安全与体验兼顾 |

---

## 三、版本演进

### 3.1 七个版本的迭代路径

```
v1.0 (6/11)        v2.0 (6/14)        v2.1 (6/17)        v3.0 (6/22)
┌──────────┐      ┌──────────┐       ┌──────────┐       ┌──────────┐
│ 4 Agent  │ ───→ │ 8 Agent  │ ───→  │ UI/UX    │ ───→  │ LangGraph│
│ AG2 框架 │      │ Router   │       │ 优化      │       │ 全面迁移 │
│ 仅编程   │      │ Streamlit│       │ 非Python │       │ OCR      │
│ 控制台   │      │ RAG      │       │ 拦截      │       │ 手动车道 │
└──────────┘      └──────────┘       └──────────┘       └──────────┘

    v3.1 (6/23)       v3.2 (6/24)       v3.3 (6/24)
   ┌──────────┐      ┌──────────┐      ┌──────────┐
   │ FastAPI  │ ───→ │ daisyUI  │ ───→ │ 用户认证 │
   │ Web 迁移 │      │ 前端重构 │      │ 数据隔离 │
   │ Router恢复│      │ SQLite   │      │ bcrypt   │
   │ exitcode │      │ 持久化   │      │ Token    │
   └──────────┘      └──────────┘      └──────────┘
                                              │
                    v3.4 (6/24) ← 当前版本     │
                   ┌──────────┐               │
                   │ Docker   │ ←─────────────┘
                   │ 沙箱隔离 │
                   │ 登录限流 │
                   │ Token过期│
                   │ 续期机制 │
                   └──────────┘
```

### 3.2 版本对比

| 版本 | Agent | 框架 | 前端 | 持久化 | 认证 | 用户隔离 | API |
|------|:-----:|------|------|--------|:----:|:--------:|:---:|
| v2.1 | 8+Router | AG2 | Streamlit | session | 无 | 无 | 0 |
| v3.0 | 7 | LangGraph | Streamlit | session | 无 | 无 | 0 |
| v3.1 | 7+Router | LangGraph | FastAPI+Bootstrap | localStorage | 无 | 无 | 7 |
| v3.2 | 7+Router | LangGraph | FastAPI+daisyUI | SQLite | 无 | 无 | 18 |
| **v3.3** | **7+Router** | **LangGraph** | **FastAPI+daisyUI** | **SQLite** | **bcrypt+Token** | **会话+知识库** | **22** |
| **v3.4** | **7+Router** | **LangGraph** | **FastAPI+daisyUI** | **SQLite** | **bcrypt+Token+过期+限流** | **会话+知识库** | **22** |

---

## 四、系统架构

### 4.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                      FastAPI Web 前端层                                │
│  ┌─────────────────────┐  ┌────────────────────────────────────────┐ │
│  │ 侧边栏（daisyUI      │  │ 主聊天区域                              │ │
│  │  drawer 抽屉式）     │  │  · 对话气泡 + Agent 卡片折叠             │ │
│  │  · 执行模式选择      │  │  · 报告下载                              │ │
│  │  · 角色/模型管理     │  │  · 生成文件展示                          │ │
│  │  · 知识库管理        │  │  · speaking_log 流转追踪                 │ │
│  │  · 用户/注册/登录    │  │                                          │ │
│  │  · 会话历史          │  │                                          │ │
│  └─────────────────────┘  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    app/chat.py — 聊天管道                              │
│  lane_mode == "auto":  Router 分类 → 关键词覆写 → 轻/重 → 快/慢车道    │
│  lane_mode == "fast":  Bot 直接回复（< 3s）                            │
│  lane_mode == "slow":  7 Agent 完整流水线（90s 超时守护）               │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                 workflow.py — LangGraph StateGraph                     │
│  快车道:  START ──→ bot_node ──→ END                                  │
│  慢车道:  planner → retriever → [coder|writer] → executor             │
│                                    ↑      │         │                  │
│                                    │   exitcode=0    ↓                  │
│                                    │      │      tester                │
│                                    │      │     ✅/❌                   │
│                                    │◄─ 修复(≤2轮)  │                   │
│                                    └──────────→ summarizer → END      │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                  app/auth.py — 认证中间件层                             │
│  require_auth(request) → Bearer Token → get_user_by_token()           │
│  ├─ 无效/缺失 Token → 401                                             │
│  ├─ 会话 API → 校验 user_id 归属 → 403                                │
│  └─ 知识库 API → 按 user_id 路由到私有目录                              │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                       持久化 & 基础设施层                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ ┌──────────┐            │
│  │ SQLite   │ │ ChromaDB │ │ sessionStorage│ │ subprocess│           │
│  │ 用户+会话│ │ 向量检索 │ │ 游客会话      │ │ 代码沙箱 │            │
│  │ +Token   │ │(按用户隔离)│ │              │ │          │            │
│  └──────────┘ └──────────┘ └──────────────┘ └──────────┘            │
└──────────────────────────────────────────────────────────────────────┘
```

### 4.2 用户认证与隔离架构

```
                   ┌─────────────────────────────────────────┐
                   │            用户访问系统                    │
                   └─────────────────────────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    ▼                                   ▼
            ┌──────────────┐                  ┌──────────────────┐
            │   游客（无Token）│                 │ 注册用户（有Token）  │
            └──────────────┘                  └──────────────────┘
                    │                                   │
        ┌───────────┼───────────┐           ┌───────────┼───────────┐
        ▼           ▼           ▼           ▼           ▼           ▼
    ┌───────┐ ┌────────┐ ┌──────────┐ ┌────────┐ ┌────────┐ ┌──────────┐
    │ 聊天  │ │ 会话    │ │ 知识库   │ │ 聊天   │ │ 会话   │ │ 知识库   │
    │ 可用  │ │session │ │ 401     │ │ 可用   │ │SQLite  │ │私有目录  │
    │      │ │Storage │ │ 拒绝    │ │       │ │归属隔离│ │物理隔离  │
    └───────┘ └────────┘ └──────────┘ └────────┘ └────────┘ └──────────┘
                                              │
                                    ┌─────────┴─────────┐
                                    ▼                   ▼
                              ┌──────────┐        ┌──────────┐
                              │ 登录 →   │        │ 退出 →   │
                              │ 迁移对话 │        │ 清除Token│
                              │ 获得Token│        │ 清空界面 │
                              └──────────┘        └──────────┘
```

### 4.3 认证数据流

```
注册：POST /api/auth/register {name, email, password}
  → bcrypt.hashpw(password)
  → INSERT INTO users (id, name, email, password_hash)
  → INSERT INTO auth_tokens (uuid4, user_id)
  → 创建 rag/documents/<user_id>/ 和 rag/chroma_db/<user_id>/
  → 返回 {token, user_id, name}

登录：POST /api/auth/login {name, password}
  → SELECT FROM users WHERE name = ?
  → bcrypt.checkpw(password, hash)
  → INSERT INTO auth_tokens
  → 返回 {token, user_id, name}

鉴权：每个请求 → Authorization: Bearer <token>
  → require_auth() → SELECT user_id FROM auth_tokens WHERE token = ?
  → 有效 → 注入 user_id 到路由处理函数
  → 无效 → 401
```

### 4.4 Router 意图分类器

| 标签 | 典型场景 | 复杂度 |
|------|---------|:------:|
| 编程 | "写一个快排"、"实现 LRU Cache" | 重 |
| 写作 | "写一份市场分析报告" | 重 |
| 分析 | "分析 sales_data.csv" | 重 |
| 问答 | "什么是机器学习" | 轻 |
| 闲聊 | "你好"、"你是谁" | 轻 |

**三层关键词覆写安全网：**

| 优先级 | 检测规则 | 触发词 | 覆写动作 |
|:------:|----------|--------|-----------|
| 1 | 搜索意图 | 搜索/查资料/检索/基于知识库 | complexity → 重 |
| 2 | 分析意图 | 以"分析"开头 | complexity → 重 + task_type → 分析 |
| 3 | 非 Python 语言 | C/Java/Rust/Go/C++/C# | task_type → 问答 + complexity → 轻 |

### 4.5 LangGraph 工作流状态

```python
class WorkflowState(TypedDict):
    user_input: str          # 用户输入
    lane_mode: str           # "auto" | "fast" | "slow"
    task_type: str           # "编程" | "写作" | "分析" | "问答" | "闲聊"
    complexity: str          # "轻" | "重"
    plan: str                # Planner 步骤计划
    knowledge: str           # Retriever 检索结果
    code_or_draft: str       # Coder/Writer 产出
    execution_result: str    # Executor 结果
    test_result: str         # Tester 评审
    fix_count: int           # 修复轮数（上限 2）
    agent_messages: list     # Agent 消息累积
    speaking_log: list       # 流转日志
    final_output: str        # 最终输出
```

### 4.6 数据库设计

```sql
-- 用户表（bcrypt 密码哈希）
CREATE TABLE users (
    id         TEXT PRIMARY KEY,
    name       TEXT NOT NULL UNIQUE,
    email      TEXT DEFAULT '',
    password   TEXT NOT NULL DEFAULT '',
    created_at TEXT DEFAULT (datetime('now', 'localtime'))
);

-- 认证令牌表
CREATE TABLE auth_tokens (
    token      TEXT PRIMARY KEY,
    user_id    TEXT NOT NULL,
    created_at TEXT DEFAULT (datetime('now', 'localtime')),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 会话表（外键约束 + 归属校验）
CREATE TABLE sessions (
    id         TEXT PRIMARY KEY,
    user_id    TEXT NOT NULL,
    title      TEXT DEFAULT '',
    messages   TEXT DEFAULT '[]',
    updated_at TEXT DEFAULT (datetime('now', 'localtime')),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

## 五、关键设计决策

### 5.1 决策总览

| # | 决策点 | 选择 | 核心理由 |
|---|--------|------|----------|
| 1 | 多 Agent 框架 | **LangGraph** 替代 AG2 | 显式 StateGraph，可调试可扩展 |
| 2 | 前端框架 | **FastAPI Web** 替代 Streamlit | 标准 HTTP，完全 UI 控制 |
| 3 | 意图分类 | **Router LLM + 关键词覆写** | 自动化覆盖 85% + 安全网兜底 15% |
| 4 | 执行判断 | **exitcode 确定性分支** | 不受 LLM 幻觉影响 |
| 5 | CSS 框架 | **daisyUI 5** 替代 Bootstrap | 语义组件更丰富 |
| 6 | 数据持久化 | **SQLite WAL** 替代 localStorage | 跨设备、多用户、并发支持 |
| 7 | 密码安全 | **bcrypt** 替代明文/localStorage | 服务端哈希，不可逆 |
| 8 | 认证机制 | **UUID Token** 替代 JWT | 轻量，无过期管理开销，适合答辩项目 |
| 9 | 知识库隔离 | **物理分目录** 替代元数据标记 | 删用户 = 删目录，零残留 |
| 10 | 代码执行沙箱 | **Docker 容器** 替代 subprocess | 完全隔离，不可用时自动降级 |
| 11 | Token 过期策略 | **7 天过期 + 活跃续期** | 兼顾安全与会话连续性 |
| 12 | 登录限流 | **5 次失败 / 15 分钟锁定** | 防暴力破解，内存计数器零 DB 开销 |

### 5.2 决策详解

**决策 7：bcrypt 密码哈希**

v3.2 的"注册"本质是 localStorage 存明文邮箱密码，安全风险高。v3.3 升级为：
- 服务端 bcrypt 哈希存储，密码原文永不留存
- `bcrypt.hashpw(password, bcrypt.gensalt())` 自动生成随机盐
- `bcrypt.checkpw()` 恒时比较，防时序攻击
- 前端不再存密码，仅存 Token

**决策 8：UUID Token vs JWT**

JWT 需要过期刷新、签名密钥管理。本项目是答辩/实习场景，UUID Token 更合适：
- 实现简单：`str(uuid.uuid4())` 即生成，数据库即状态
- 退出即删除：DELETE FROM auth_tokens WHERE token = ?，无需黑名单
- 零配置：不需要 JWT_SECRET 环境变量

**决策 9：知识库物理隔离 vs 元数据标记**

物理方案的优势：
- 每个用户独立的 ChromaDB PersistentClient 和目录
- 检索时天然隔离，不需要 `WHERE user_id = ?` 过滤
- 删除用户：`rm -rf rag/documents/<user_id>/ rag/chroma_db/<user_id>/`

**决策 10：Docker 沙箱 vs subprocess**

v3.3 使用 subprocess 执行代码，仅提供进程级隔离，代码可访问宿主机文件系统。v3.4 升级为 Docker 容器：
- 完全文件系统隔离，容器内无法触碰宿主机文件
- 默认无网络 (`network_mode: none`)，防止恶意外连
- CPU / 内存资源限制，防止资源耗尽
- 降级策略：Docker 不可用时自动回退 subprocess，系统不中断

**决策 11：Token 7 天过期 + 自动续期**

v3.3 的 UUID Token 永不过期，一旦泄露可被永久利用。v3.4 引入：
- `auth_tokens` 表新增 `expires_at` 字段，创建时设为 7 天后
- 每次 API 鉴权成功自动将 `expires_at` 更新为当前时间 + 7 天
- 过期 Token 返回 401，用户需重新登录
- 活跃用户无感知，长期不使用自动回收

**决策 12：登录限流 5 次 / 15 分钟**

v3.3 登录接口无频率限制，可被脚本暴力破解。v3.4 引入：
- 服务端内存字典 `{username: [attempt_times]}`，零数据库开销
- 同一用户名 5 次失败后 15 分钟内返回 429 "Too many attempts"
- 15 分钟窗口到期自动清除，无需定时任务
- 不限制不同用户名尝试（无法通过锁定他人账号制造 DoS）

**游客 sessionStorage vs 数据库**

v3.2 将所有用户（含游客）的会话都存入 SQLite。v3.3 改为：
- 游客会话 → sessionStorage（关标签页即清除，不占数据库空间）
- 注册用户会话 → SQLite（持久化、跨设备）
- 登录时自动迁移：sessionStorage → POST /api/sessions

---

## 六、功能验证

### 6.1 Router 分类实测

| 用户输入 | Router 判定 | 车道 | 结果 |
|----------|------------|:----:|------|
| "你好" | 闲聊\|轻 | 🚀 快 | Bot 友好问候 |
| "什么是机器学习" | 问答\|轻 | 🚀 快 | Bot 简洁解释 |
| "写一个快速排序" | 编程\|重 | 🔄 慢 | Coder → Executor → Tester → 报告 |
| "分析 sales_data.csv" | 分析\|重 | 🔄 慢 | Coder pandas → Executor → 图表 |
| "写一份市场分析报告" | 写作\|重 | 🔄 慢 | Writer → Tester → Summarizer |

### 6.2 用户认证实测

| 操作 | API 调用 | 结果 |
|------|----------|------|
| 注册 | `POST /api/auth/register {name, email, password}` | 返回 `{token, user_id, name}`，"用户名已存在" → 409 |
| 登录 | `POST /api/auth/login {name, password}` | 返回 `{token, user_id, name}`，密码错误 → 401 |
| 获取用户 | `GET /api/auth/me`（带 Token） | 返回 `{user_id, user_name}` |
| 退出 | `POST /api/auth/logout`（带 Token） | Token 从数据库删除 |
| 无效 Token | 任意需鉴权的 API | 401 "令牌无效或已过期" |

### 6.3 用户隔离实测

| 测试场景 | 操作 | 结果 |
|----------|------|------|
| 跨用户会话 | 用户 A 的 Token 访问用户 B 的 session_id | 403 "无权访问" |
| 跨用户删除 | 用户 A 试图删除用户 B 的会话 | 403 |
| 知识库隔离 | 用户 A 上传文档，用户 B 查看 stats | 各自看到自己的文档数 |
| 知识库文件 | 用户 A 上传 test.txt，用户 B 删除 test.txt | 用户 B 返回 404（文件不存在） |

### 6.4 游客模式实测

| 操作 | 结果 |
|------|------|
| 首次访问（未登录） | 显示"游客"，可正常发送消息 |
| 发送消息后刷新页面 | 对话保留（sessionStorage） |
| 关闭标签页重开 | 对话消失（sessionStorage 清除） |
| 知识库操作 | 返回 401（游客无权访问） |
| 游客发消息 → 注册登录 | 对话迁移到侧栏历史 |
| 登录后刷新 | 对话从数据库恢复 |

### 6.5 自动化测试

```bash
$ pytest tests/ -v
tests/test_knowledge_routes.py::test_auth_register PASSED
tests/test_knowledge_routes.py::test_auth_login PASSED
tests/test_knowledge_routes.py::test_auth_me PASSED
tests/test_knowledge_routes.py::test_kb_stats PASSED
tests/test_knowledge_routes.py::test_kb_rebuild PASSED
tests/test_knowledge_routes.py::test_kb_delete_not_found PASSED
tests/test_knowledge_routes.py::test_kb_upload_reject_invalid_type PASSED
tests/test_knowledge_routes.py::test_kb_delete_success PASSED
tests/test_knowledge_routes.py::test_kb_upload_text_file PASSED
======================= 9 passed =======================
```

---

## 七、v3.4 核心亮点

### 7.1 Docker 沙箱隔离（subprocess → Docker）

代码执行从 subprocess 进程级隔离升级为 Docker 容器级完全隔离：

| 对比维度 | v3.3 | v3.4 |
|----------|------|------|
| 隔离方式 | subprocess 进程 | Docker 容器 |
| 文件系统 | 共享宿主机 | 独立容器文件系统 |
| 网络访问 | 无限制 | 默认无网络（`network_mode: none`） |
| 资源限制 | 无 | CPU / 内存上限 |
| 降级策略 | 无 | Docker 不可用时自动降级 subprocess |

**降级设计：** 系统启动时检测 Docker 可用性。Docker 可用时所有代码在容器中运行；Docker 不可用时自动回退 subprocess，确保系统不中断。

### 7.2 登录限流（防暴力破解）

v3.3 的登录接口无频率限制，存在暴力破解风险。v3.4 引入内存级限流：

| 对比维度 | v3.3 | v3.4 |
|----------|------|------|
| 失败限制 | 无 | 同一用户名 5 次 / 15 分钟 |
| 存储方式 | 无 | 服务端内存字典，零 DB 开销 |
| 锁定行为 | 无 | 返回 429 "Too many attempts" |
| 解锁条件 | 无 | 15 分钟后自动解除 |

### 7.3 Token 过期与自动续期

v3.3 的 UUID Token 永不过期，存在长期泄露风险。v3.4 引入过期+续期机制：

| 对比维度 | v3.3 | v3.4 |
|----------|------|------|
| Token 有效期 | 永久 | 7 天 |
| 过期行为 | 无 | 返回 401 "令牌已过期" |
| 续期机制 | 无 | 每次 API 调用自动续期 7 天 |
| 实现方式 | 无 | `auth_tokens.expires_at` 字段 + 中间件自动更新 |

**设计考量：** 7 天过期保证即使 Token 泄露影响有限；活跃使用自动续期避免用户频繁登录，兼顾安全与体验。

### 7.4 安全防护体系升级

| 防护层 | v3.3 | v3.4 | 实现 |
|--------|:----:|:----:|------|
| Docker 沙箱 | -- | ✅ | 容器隔离执行 |
| 密码哈希 | ✅ | ✅ | bcrypt + 随机盐 |
| 登录限流 | -- | ✅ | 5 次 / 15 分钟 |
| SQL 注入 | ✅ | ✅ | 100% 参数化查询 |
| 路径遍历 | ✅ | ✅ | `os.path.basename()` |
| Docker 降级 | -- | ✅ | 不可用时自动回退 |
| Token 鉴权 | ✅ | ✅ | UUID Bearer Token |
| Token 过期 | -- | ✅ | 7 天自动过期 |
| Token 续期 | -- | ✅ | 活跃使用自动续期 |
| 会话归属校验 | ✅ | ✅ | 403 拦截跨用户访问 |

安全防护从 v3.3 的 7 层提升至 **10 层**。

---

## 八、技术栈

| 领域 | 技术选型 | 选型理由 |
|------|----------|----------|
| **Agent 编排** | LangGraph ≥0.2 | 显式 StateGraph，条件路由 |
| **Agent 实现** | LangChain ≥0.3 | ChatDeepSeek + @tool |
| **大语言模型** | DeepSeek V4 Flash | 全部 Agent + Router 统一使用 |
| **Web 框架** | FastAPI ≥0.115 | 异步高性能，22 个 API 端点 |
| **模板引擎** | Jinja2 ≥3.1 | 服务端渲染 |
| **CSS 框架** | daisyUI 5 + Tailwind CSS 4 | 语义组件 + 30+ 主题 |
| **前端交互** | 原生 JavaScript | fetch + DOM + sessionStorage |
| **数据库** | SQLite（标准库 sqlite3） | WAL 模式，零依赖 |
| **密码哈希** | bcrypt ≥4.0 | 工业标准，恒时比较 |
| **容器沙箱** | Docker | 代码执行隔离，不可用时自动降级 |
| **向量数据库** | ChromaDB ≥1.5 | 本地持久化，按用户目录隔离 |
| **嵌入模型** | BAAI/bge-small-zh-v1.5 | 33MB，离线加载，中文优化 |
| **测试框架** | pytest + FastAPI TestClient | 9 个自动化测试 |

---

## 九、总结与展望

### 9.1 项目成果汇总

| 指标 | v3.1 | v3.2 | v3.3 | v3.4 |
|------|:----:|:----:|:----:|:----:|
| 源文件数 | 22 | 24 | 26 | **27** |
| Agent 数量 | 7+Router | 7+Router | 7+Router | 7+Router |
| API 端点 | 7 | 18 | 22 | **22** |
| 测试用例 | 6 | 6 | 9 | **9** |
| 数据持久化 | localStorage | SQLite | SQLite + Token | SQLite + Token |
| 用户系统 | 无 | 幂等注册 | bcrypt + Token + 隔离 | **+ 过期 + 限流** |
| 知识库 | 全局共享 | 全局共享 | 按用户物理隔离 | 按用户物理隔离 |
| 游客模式 | 数据库存储 | 数据库存储 | sessionStorage | sessionStorage |
| 安全防护 | 基础 | 4 层 | 7 层 | **10 层** |

### 9.2 关键收获

1. **LangGraph 显式图 > 黑盒调度**：工作流即文档，加分支一行代码
2. **确定性信号 > LLM 判断**：exitcode 比 LLM 文字判断可靠得多
3. **标准 Web > 框架锁定**：FastAPI + Jinja2 + daisyUI，任何 Python 开发者都能维护
4. **持久化是产品分水岭**：SQLite + WAL 让系统从 Demo 升级为可用产品
5. **认证是数据隔离的前提**：有了可靠的用户身份，会话和知识库隔离才有意义
6. **物理隔离 > 逻辑过滤**：分目录比加 WHERE 子句更简单、更安全、更直观
7. **游客也是用户**：sessionStorage 让游客体验流畅，登录后迁移让数据不丢失

### 9.3 后续可改进方向

| 方向 | 描述 | 优先级 |
|------|------|:------:|
| **WebSocket 流式推送** | 逐 Agent 推送进展，提升感知速度 | 高 |
| **并行 Agent 执行** | LangGraph `Send()` API 并行化独立任务 | 中 |
| **会话密码修改** | 添加密码修改和找回功能 | 中 |
| **JWT 升级** | 生产环境可用 JWT 替代 UUID Token | 低 |
| **长期记忆层** | Mem0 或 LangChain Memory | 低 |

---

*报告生成日期：2026-06-24 | 版本：v3.4 | 西北农林科技大学 2026 人工智能实训*
